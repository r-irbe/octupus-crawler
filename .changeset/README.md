# Changesets

This directory is used by [Changesets](https://github.com/changesets/changesets) to track version bumps.

When making changes to packages, run `pnpm changeset` to create a changeset file describing what changed.
The "Version Packages" PR automation will bump versions and update changelogs on merge.
