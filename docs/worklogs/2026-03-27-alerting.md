# Worklog: Alerting Implementation

| Field | Value |
| --- | --- |
| Date | 2026-03-27 |
| Branch | `work/alerting` |
| Spec | `docs/specs/alerting/` |
| Commits | 709e4c4, 65978b6 |

## Summary

Implemented Prometheus alert rules, promtool tests, and Alertmanager routing configuration for the IPF crawler. All 17 requirements (REQ-ALERT-001–017) addressed.

## Files Created

| File | Purpose | Lines |
| --- | --- | --- |
| `infra/prometheus/alert-rules.yml` | 12 PromQL alert rules | 179 |
| `infra/prometheus/alert-rules-fetch.test.yml` | Tests: HighErrorRate, ZeroFetchRate, StalledJobs, P95/P99 + edge cases | 256 |
| `infra/prometheus/alert-rules-ops.test.yml` | Tests: Frontier, Utilization, WorkerDown, CoordinatorRestart, ZeroDiscovery + edge cases | 289 |
| `infra/alertmanager/alertmanager.yml` | Severity-based routing config (template) | 62 |
| `docs/memory/session/2026-03-27-alerting-state.md` | State tracker | — |

## Files Modified

| File | Change |
| --- | --- |
| `package.json` | Added `test:alerts` CI script |

## Key Decisions

| # | Decision | Rationale |
| --- | --- | --- |
| D1 | Used `sum()` in HighErrorRate PromQL | Prevents label leakage from `rate()` retaining status label |
| D2 | Used `on()` in ZeroFetchRate/ZeroDiscovery | Allows `and` to match across series with different label sets |
| D3 | Split tests into 2 files | 300-line hard limit per file |
| D4 | 15s input interval for CoordinatorRestart test | `increase()` needs >=2 samples in 1m lookback window |

## Problems Encountered

| # | Problem | Resolution |
| --- | --- | --- |
| P1 | HighErrorRate label leakage: rate() retains status label | Wrapped rate() in sum() to drop labels before division |
| P2 | `and` label matching failure in ZeroFetchRate/ZeroDiscovery | Added `on()` to ignore label differences |
| P3 | CoordinatorRestart not firing in tests | `increase()` with 1m interval only gets 1 sample in 1m window; used 15s interval for test |
| P4 | promtool requires exact annotation matching | Added full exp_annotations in all "should fire" tests |

## RALPH Review

- Round 1: 8 findings (F-001–F-008)
- Round 2 Deliberation: F-002 rejected, F-001 downgraded to Minor
- Round 3 Vote: 4 sustained (F-001, F-004, F-005, F-007)
- F-001: Resolved — added deployment template documentation comment
- F-004: Deferred to G11 — design.md needs sum()/on() PromQL updates
- F-005: Deferred to G11 — Mermaid diagram fix (AJ→R10 should be up→R10)
- F-007: Resolved — added 6 metric-absence edge case tests
- Re-review: APPROVED

## Test Summary

- 12 alert rules validated with `promtool check rules`
- 30+ test cases across 2 test files
- Test categories: fire tests (12), no-fire tests (12), edge cases (6+), metric absence (6)

## Deferred Items

- F-004/F-005 spec updates → G11
- Duration boundary edge cases → limited by promtool evaluation_interval granularity
